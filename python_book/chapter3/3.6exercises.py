# min will produce zero, max will produce n - 1

def num_digits(s):
    return (len(str(s)))

def problem3(s):
    return (sum(int(i) for i in str(s)))

# an error is created, as list is unhashable
# this works and returns 1, as the tuple is one element

# 23 is the result, as they are strings yet to be converted to integers.

# 2 to the 8th power modulus 5, which yields 1

# it will produce the set of numerically sorted square roots of 1 through 19 rounded to the nearest whole number.